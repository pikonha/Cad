#include "CmdWheelEvent.h"

void CmdWheelEvent::execute(Data& d, MainScreen& s)
{
	double scaleF = 1.15;

	//if (event->delta() > 0)
	//	scale(scaleF, scaleF);
	//else
	//	scale(1 / scaleF, 1 / scaleF);
}